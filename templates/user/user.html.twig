

{% block body %}
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css">
<style>
    .card {
        box-shadow: 0 4px 8px 0 rgba(0, 0, 0, 0.2);
        max-width: 300px;
        margin: auto;
        text-align: center;
    }

    .title {
        color: grey;
        font-size: 18px;
    }

    button {
        border: none;
        outline: 0;
        display: inline-block;
        padding: 8px;
        color: white;
        background-color: #000;
        text-align: center;
        cursor: pointer;
        width: 100%;
        font-size: 18px;
    }

        a {
            text-decoration: none;
            font-size: 22px;
            color: Red;

    button:hover, a:hover {
        opacity: 0.7;
    }

</style>
    <div class="card">
        <img src='{{ '/Uploads/user/usericon.png' }} ' alt="John" style="width:100%">
        #{% for user in users %} <h1>{{ user.email }}</h1>{% endfor %}#
        <p class="title">Normal user</p>
        <p><button><a href="{{ path('app_homepage') }}">Back to Home Page</a></button></p>
    </div>
    <div class="container">
        <h2>You can try to make new review <a href="{{ path('app_create') }}">New </a></h2>
        <h2>Or you can continue review the blog <a href="{{ path ('app_review') }}">View</a></h2>
    </div>


{% endblock %}
